import 'package:flutter/material.dart';

class ChartModel {
  final String title;
  final double percent;
  final Color color;

  const ChartModel({
    required this.title,
    required this.percent,
    required this.color,
  });
}
